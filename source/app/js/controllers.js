
// Declare module.
var appCtrl = angular.module('myApp.Controllers', ['toastr']);

// Controller for the navigation bar.
appCtrl.controller('NavbarCtrl', function ($scope) {
    
});

// Controller for the home page.
appCtrl.controller('IntroCtrl', function ($scope) {
    
});

// Controller for the login view.
appCtrl.controller('LoginCtrl', function ($scope) {

});
